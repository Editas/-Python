[tool.poetry]
name = "web-python-env"
version = "0.1.0"
description = "Browser-based reproducible Python env (Codespaces/Gitpod)"
package-mode = true


[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.115.0"
uvicorn = {extras = ["standard"], version = "^0.30.0"}
pytest = "^8.3.0"
pytest-cov = "^5.0.0"


[tool.poetry.group.dev.dependencies]
black = "^24.8.0"
ruff = "^0.5.7"
mypy = "^1.10.0"
pre-commit = "^3.8.0"


[tool.black]
line-length = 100


[tool.ruff]
line-length = 100
select = ["E", "F", "I", "B", "UP"]


[tool.mypy]
python_version = "3.11"
strict = true


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
